import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mx.com.mueblesmessa.app',
  appName: 'egret',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
